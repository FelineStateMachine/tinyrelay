// Package event defines the relay's Nostr event and filter values. It owns
// wire parsing, canonical serialization, Schnorr signing and signature
// validation, plus the common kind and tag helpers used by storage and
// protocol gates.
package event
